import type { AnalyticsAudienceReport } from "@shared/schema";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { ChartContainer, ChartTooltip, ChartTooltipContent } from "@/components/ui/chart";
import { Cell, Pie, PieChart } from "recharts";

export function AnalyticsAudience({ audience }: { audience: AnalyticsAudienceReport }) {
  const data = [{ name: "New visitors", value: audience.newVisitors, fill: "#e07a5f" }, { name: "Returning visitors", value: audience.returningVisitors, fill: "#3d405b" }];
  return <Card><CardHeader><p className="text-xs font-semibold uppercase tracking-[0.16em] text-primary">Audience</p><CardTitle className="text-xl">New meets familiar</CardTitle></CardHeader><CardContent className="grid items-center gap-5 sm:grid-cols-[150px_1fr]"><ChartContainer config={{ new: { label: "New visitors", color: "#e07a5f" }, returning: { label: "Returning visitors", color: "#3d405b" } }} className="mx-auto h-[150px] w-[150px] aspect-auto"><PieChart><ChartTooltip content={<ChartTooltipContent />} /><Pie data={data} dataKey="value" nameKey="name" innerRadius={48} outerRadius={70} paddingAngle={3}>{data.map((entry) => <Cell key={entry.name} fill={entry.fill} />)}</Pie></PieChart></ChartContainer><div className="space-y-3">{data.map((entry) => <div key={entry.name} className="flex items-center justify-between border-b border-border/60 pb-2 text-sm last:border-0"><span className="flex items-center gap-2"><i className="h-2.5 w-2.5 rounded-full" style={{ backgroundColor: entry.fill }} />{entry.name}</span><strong>{entry.value.toLocaleString()}</strong></div>)}<p className="text-sm text-muted-foreground"><strong className="text-foreground">{audience.returningVisitorRate.toFixed(1)}%</strong> of visitors came back.</p><div className="grid grid-cols-2 gap-3 text-xs text-muted-foreground"><span>New sessions <strong className="block text-sm text-foreground">{audience.newSessions.toLocaleString()}</strong></span><span>Returning sessions <strong className="block text-sm text-foreground">{audience.returningSessions.toLocaleString()}</strong></span></div></div></CardContent></Card>;
}