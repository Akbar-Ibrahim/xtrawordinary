import type { AnalyticsGameReport, Game } from "@shared/schema";
import { Link } from "wouter";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function AnalyticsGames({ games, catalog }: { games: AnalyticsGameReport[]; catalog: Game[] }) {
  const names = Object.fromEntries(catalog.map((game) => [game.slug, game.name]));
  return <Card><CardHeader><p className="text-xs font-semibold uppercase tracking-[0.16em] text-primary">By game</p><CardTitle className="text-xl">Where players are playing</CardTitle></CardHeader><CardContent className="overflow-x-auto">{games.length ? <table className="w-full min-w-[560px] text-sm"><thead><tr className="border-b text-left text-xs uppercase tracking-wide text-muted-foreground"><th className="px-2 py-3 font-medium">Game</th><th className="px-2 py-3 font-medium">Mode</th><th className="px-2 py-3 text-right font-medium">Starts</th><th className="px-2 py-3 text-right font-medium">Completions</th><th className="px-2 py-3 text-right font-medium">Rate</th></tr></thead><tbody>{games.map((game) => <tr key={`${game.gameSlug}-${game.gameMode}`} className="border-b last:border-0"><td className="px-2 py-3 font-medium"><Link href={`/game/${game.gameSlug}`} className="text-primary hover:underline">{names[game.gameSlug] ?? game.gameSlug}</Link></td><td className="px-2 py-3 capitalize text-muted-foreground">{game.gameMode}</td><td className="px-2 py-3 text-right tabular-nums">{game.starts.toLocaleString()}</td><td className="px-2 py-3 text-right tabular-nums">{game.completions.toLocaleString()}</td><td className="px-2 py-3 text-right font-semibold">{game.completionRate.toFixed(1)}%</td></tr>)}</tbody></table> : <div className="py-8 text-center text-sm text-muted-foreground">No game activity in this range.</div>}</CardContent></Card>;
}